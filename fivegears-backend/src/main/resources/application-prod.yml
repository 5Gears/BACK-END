spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:fivegears-db}:${DB_PORT:3306}/FiveGears?useSSL=false&serverTimezone=UTC
    username: ${DB_USER:fivegears_user}
    password: ${DB_PASSWORD:senhaSegura}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # apenas valida o schema, não altera
    show-sql: false

server:
  port: 8080

esco:
  bucket-url: "https://bucket-raw-grupo5-fivegears.s3.us-east-1.amazonaws.com/dados-esco/ESCO_cargos_competencias_limpo.xlsx"
